# Backlog

- [ ] тизер статей из `<!--more-->` и readingTime нельзя получить из
      module-глоба - решить как загружать (расширение vite-plugin-svelte-md или
      поле во frontmatter)
- [ ] английская версия для всех типов контента, позже будет переводится
      автоматически, но для mvp надо сделать копирование и пометку "требуется
      перевод" (добавить в frontmatter)
- [ ] каждый тип контента должны храниться в отдельной папке (slug? как
      обеспечить уникальность? slug с датой? или просто дата для weekly) и в ней
      должны быть файлы `index.ru.md` и `index.en.md`.
